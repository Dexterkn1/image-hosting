# Workflow ini disabled. Gunakan Cloudflare Pages native GitHub integration
# 1. Login ke https://dash.cloudflare.com/
# 2. Pages > Connect to Git > image-hosting repository
# 3. Build settings: Framework = None, Build command = npm run build, Build output directory = dist


